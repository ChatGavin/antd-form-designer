{
  "name": "antd-form-designer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev:designer": "turbo run dev --filter designer",
    "build:designer": "turbo run build --filter designer"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
